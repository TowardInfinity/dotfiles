# The old Bubble Tea model remains a compatibility seam until the planned
# internal/dots package split. Its contract tests intentionally reference
# helpers that production no longer reaches, so only this package suppresses
# U1000; all other packages retain the unused-code check.
checks = ["inherit", "-U1000"]
