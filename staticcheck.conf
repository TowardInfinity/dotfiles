# The deprecated Bubble Tea model remains compiled as a compatibility seam for
# its focused contract tests until the planned internal/dots package split.
# Staticcheck's U1000 marks symbols used only by those tests as dead; keep the
# correctness, security, and simplification analyzers active while that seam
# is intentionally retained.
checks = ["all", "-U1000"]
