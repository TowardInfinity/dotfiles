# Keep every analyzer enabled for the repository. The deprecated Bubble Tea
# model's narrow exemption lives in cmd/dots/staticcheck.conf, where the
# compatibility seam actually exists.
checks = ["all"]
