# Go development tools used by this repository.
#
# Keep these pins in one shell-readable file. CI, the local lint wrapper, and
# bootstrap all consume this file so a tool upgrade cannot silently leave one
# of those paths on a different analyzer release.
STATICCHECK_MODULE='honnef.co/go/tools/cmd/staticcheck'
STATICCHECK_VERSION='v0.7.0'
